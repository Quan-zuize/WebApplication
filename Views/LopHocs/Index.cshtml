@model IEnumerable<WebApplication2.Models.LopHoc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LopHocName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LopHocName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LopHocId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LopHocId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LopHocId })
        </td>
    </tr>
}

</table>
